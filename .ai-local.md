# Cross-Agent Shared State

All AI tools read this file at session start. Keep under 50 lines. Tracked in git.

## Direction & status (2026-07-05)

Build multiple Asterisk versions as native Alpine `apk` from one Docker
buildchain. **Strategy: single Alpine base (3.24) for every version.** Full
goals, build-list table, and per-version findings in **`ROADMAP.md`**.

**GREEN (8 versions, 141 APKs in `repository/v3.24/main/x86_64/`, signed):**
23.4.1, 22.10.1, 22.8-cert3 (certified), 20.20.1, 18.26.4, 17.9.4, 16.30.1, 15.7.4
All pass `make test-all`: install from repo → `asterisk -V` → daemon runs →
modules load → HEP (res_hep/res_hep_pjsip/res_hep_rtcp) on disk + res_hep loads.

**FAILURE FRONTIER = 14.x.** 14.7.8 fails: pjproject removed `struct pj_in_addr`
(res_pjsip/pjsip_resolver.c). 13.38.3 fails earlier: db1-ast `HTAB.mapp` gone.
≤12 expected same/worse.

## Tests

`make test-all` / `make test-<line>`. Files: `docker/test.Dockerfile`,
`scripts/test-run.sh`. Certified uses `-e RELAXED=1` (reports
`certified-22.8-cert3`, not pkgver). 15.x res_hep ships but won't load (symbol
relocation) → WARN not FAIL.

## Key lessons (don't relearn)

- abuild 3.17 (3.24) needs `/etc/apk/cache` to exist (builder + test images).
- 22+ removed chan_sip from source; `--enable chan_sip` fails the build.
- `40-asterisk-cdefs.patch` & `20-musl-astmm-fix.patch` drift per line (15/23
  trim db1-ast hunk; 14.x astmm context under #ifdef HAVE_* guards).
- ≤15 lack the `install-headers` make target — guard with `|| true`.
- `update_config_guess`/`update_config_sub` abort prepare on abuild 3.17 — drop.
- abuild `$repo` = parent dir of APKBUILD; mount at main/asterisk, set REPODEST.
- noarch subpackages (sample-config, sounds) in x86_64/ fail apk 3.0 repo
  resolution — install by direct .apk path in tests.
- certified 22.8 + libpq 18: pgsql/ldap/tds/prometheus don't build (omit);
  default menuselect excludes HEP — force-enable res_hep*.
- 15.x hard-fails at startup if data dirs missing (sounds/moh/keys) — create;
  its res_hep ships but won't load (symbol relocation vs modern toolchain).

## Recent Changes

2026-07-05 | tests | make test-all PASS for all 8 versions; HEP enabled on certified
2026-07-05 | M2 | 8 green on 3.24; frontier at 14.x (pjproject); 141 APKs
2026-07-05 | M1 | matrix restructure: packages/<line>/, buildchain/versions.mk
2026-07-05 | docs | ROADMAP.md with goals, milestones, build list, risks

## Active Warnings

- `packages/*/src/`, `packages/*/pkg/`, `repository/` are gitignored output.
- DO NOT commit `keys/*.rsa` (private key). Public key is fine to ship.

## Inter-Agent Notes

Resurrect SHAs for ≤11 (aports history): 1.8 `bd673b51a11a`, 10 `6e8ed58d7cee`,
11 `2.7-stable`. Expected to fail on 3.24; only useful if revisiting multi-base.
