include depreciated cdefs.h as needed for compilation with alpine
(Asterisk 15.7.4's utils/db1-ast/include/db.h already includes sys/cdefs.h,
so only the ast_expr2.c hunk is needed.)

--- a/main/ast_expr2.c
+++ b/main/ast_expr2.c
@@ -95,6 +95,7 @@
 #include "asterisk.h"

 #include <sys/types.h>
+#include <sys/cdefs.h>
 #include <stdio.h>

 #if defined(STANDALONE) || defined(STANDALONE2)
