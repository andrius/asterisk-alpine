Add include file needed when compiling with musl
(Asterisk 14 wraps includes in #ifdef HAVE_* guards, so the context differs
from newer versions.)

--- a/include/asterisk/compat.h
+++ b/include/asterisk/compat.h
@@ -33,6 +33,7 @@

 #ifdef HAVE_UNISTD_H
 #include <unistd.h>
 #endif
+#include <pthread.h>

 #ifdef HAVE_STDDEF_H
 #include <stddef.h>
